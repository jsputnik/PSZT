#include <iostream>
#include "HelloWorld.h"

using namespace std;

int main() {
    int pm = 0;
    int pc = 0;
    int mu = 0;
    int t = 0;
    hello_world();
    /*p = initialise();
    grade(p0);
    while(!end) {
        t = selection(p);
        o = crossing_and_mutation(t);
        garde(o);
        p = o;
        ++t;
    }*/
}
